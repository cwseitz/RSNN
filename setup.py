from setuptools import setup, find_packages

setup = setup(
    name='hebb',
    description='A package for Hebbian learning in spiking recurrent networks',
    version='1.0',
    packages=find_packages(),
)
